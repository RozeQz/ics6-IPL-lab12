<% provide :page_title, 'Редактировать' %>
<% currently_at 'Пользователи' %>

<style>
  .registration-label{
    padding-bottom: 16px;
    font-family: var(--bs-font-sans-serif);
    /* color: #0d6efd; */
  }
</style>

<h1 class="registration-label">Редактирование</h1>

<%= render 'form', user: @user %>

<%= link_to 'Профиль', @user %> |
<%= link_to 'Назад', users_path %>
